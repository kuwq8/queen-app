import ClientMessagesLayout from './ClientMessagesLayout';

export default function MessagesLayout({ children }: { children: React.ReactNode }) {
  return <ClientMessagesLayout>{children}</ClientMessagesLayout>;
}
